import 'package:flutter/material.dart';

class SephaView extends StatelessWidget {
  const SephaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(color: Colors.amberAccent);
  }
}
